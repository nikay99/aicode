# Utils module for testing imports
# Provides helper functions

let VERSION = "1.0.0"

fn greet(name)
  return "Hello, " + name + "!"

fn add(a, b)
  return a + b

fn _private_helper()
  return "This should not be exported"

export fn exported_add(a, b)
  return a + b
