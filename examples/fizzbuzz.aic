# FizzBuzz in AICode

fn fizzbuzz(n: int) -> str
  if n % 3 == 0 and n % 5 == 0
    return "FizzBuzz"
  else if n % 3 == 0
    return "Fizz"
  else if n % 5 == 0
    return "Buzz"
  else
    return n

for i in range(1, 100)
  println(fizzbuzz(i))
